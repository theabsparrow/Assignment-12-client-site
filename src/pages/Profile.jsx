import { Helmet } from "react-helmet";


const Profile = () => {
    return (
        <div>
            <Helmet>
                <title>Profile || surveyAtlas</title>
            </Helmet>
            this is profile
        </div>
    );
};

export default Profile;